package ${packageName};

import com.htstudio.core.mvvm.BaseModel;

public class ${className}Model extends BaseModel<${className}Contract.Controller> 
	implements ${className}Contract.Model {

    ${className}Model(${className}Contract.Controller controller) {
        super(controller);
	}
}
